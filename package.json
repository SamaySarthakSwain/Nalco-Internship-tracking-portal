{
  "name": "nalco-internship-tracking-portal-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "postinstall": "npm install --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
